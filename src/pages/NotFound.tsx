import { NotFoundSign } from '../components/404/index';

type Props = {};

const NotFound = (props: Props) => {
    return (
        <>
            <NotFoundSign />
        </>
    );
};

export { NotFound };
